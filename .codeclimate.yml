engines:
    rubocop:
        enabled: true
    eslint:
        enabled: true
    csslint:
        enabled: true
    bundler-audit:
        enabled: true

ratings:
    paths:
        - "lib/*.php"

exclude_paths:
    - "modules/*"
    - "tests/*"
    - "third_party/*"
    - "docs/*"
    - "language/*"
    - "website/*"
